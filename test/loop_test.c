#include "loop.h"

int main()
{
    // TODO add timers to a loop
    // timers call printf and print a time when they go off, tada
    // Add SIGUSR1(? the hell is this)
    // Add STDIN as a file descriptor and have it print off what we send along that fd

    // Convert build.sh to CMake, use OUT-OF-SOURCE BUILD, NOT IN-SOURCE BUILD
    // This will involve creating a CMakeLists.txt, and changing the build.sh to
    // prepare the build dir, run cmake, and make
}